# The CLI repo contains only the installer, the README and src/.
# Anything else at the root is a project generated by `gosite create`.
/*
!/.gitignore
!/install.sh
!/README.md
!/MEMORY.md
!/src/
!/docs/
